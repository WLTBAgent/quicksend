services:
  quicksend:
    build: .
    environment:
      - QUICKSEND_ADDR=:8080
      - QUICKSEND_DATA_DIR=/data
    volumes:
      - quicksend-data:/data
    restart: unless-stopped

volumes:
  quicksend-data:
